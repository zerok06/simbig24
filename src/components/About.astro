---
import Wrapper from '../components/Wrapper.astro'
import DATA from '../../information.json'
const {
  about: { paragraph, image, title },
} = DATA
---

<Wrapper>
  <section class="py-20">
    <div class="flex gap-8 flex-col md:flex-row">
      <div class="flex-1 flex flex-col justify-center gap-3">
        <span class="text-primary font-medium text-base"
          >[About Conference]</span
        >
        <h2 class="text-5xl font-bold">{title}</h2>
        <p class="text-balance">{paragraph}</p>
      </div>
      <div class="flex-1">
        <div class="aspect-auto h-[400px] bg-black"></div>
      </div>
    </div>
  </section>
</Wrapper>
